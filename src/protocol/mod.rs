/// all information models used during communication
pub mod model;
/// all requests during communication, using models
pub mod request;
/// all responses during communication, using models
pub mod response;
